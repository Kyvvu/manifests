# 0.9.0 renamed the rule-registry metadata key to `enforcement_points`
# and PolicyDefinition.scope to .enforcement_point. Earlier releases
# cannot validate these manifests.
kyvvu-engine>=0.9.0
pytest>=7.0
pyyaml>=6.0
yamllint>=1.0
ruff>=0.5.0
